<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Virus.exe - Login</title>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">

    <link rel="stylesheet" href="http://localhost:3000/game/mainStyle.css">
</head>
<body>

    <style>
        body {
            margin: 0;
            padding: 0;

            background: #252525;
        }
        .content {
            position: absolute;

            padding: 10px 20px;
            border-radius: 20px;

            top: 50%;
            left: 50%;

            transform: translate(-50%, -50%);

            background: #161616;

            text-align: center;
        }
        input {
            background: #252525;
            border: none;
            border-bottom: 1px solid #6b6b6b;

            padding: 7px 5px;
            outline: none;
            color: #6b6b6b;

            margin-bottom: 5px;
        }
        span {
            display: block;
            color: #6b6b6b;
        }
        h4 {
            color: white;
        }
        h5::before {
            content: '';

            width: 100%;
            height: 2px;
            background: #252525;
            margin: 5px auto;
            display: block;
        }
        a {
            padding: 5px 5px;
            color: #6b6b6b;
            text-decoration: none;
            background: #252525;
            border-radius: 10px;
        }
        input[type="submit"] {
            width: 50%;
            border-radius: 10px;
            border: none;
        }
    </style>
    
    <div class="content">
        <h5></h5>

        <h4>Virus.exe - Login</h4>

        <h5></h5>
        <form action="/login" method="post">
        
            <span><input type="text" name="login" placeholder="Login"></span>
            <span><input type="password" name="password" placeholder="Senha"></span>

            <h5></h5>
            <span><input type="submit" value="Logar"></span>
            <h5></h5>
            <span>Nao possui conta? <a href="/register">Registrar</a></span>
            <h5></h5>
            <% if ( err ) { %>
                <span style="color: #ff6565;"><%= err %></span>
                <h5></h5>
            <% } %>

        </form>
    </div>

</body>
</html>